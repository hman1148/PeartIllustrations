export enum ApiPaths {
  Auth = '/api/auth',
  Users = '/api/users',
}

export enum AuthPaths {
  Login = '/login',
  Logout = '/logout',
}

export enum UserPaths {
  Me = '/me',
  Create = '',
  Update = '',
  Delete = '',
}
